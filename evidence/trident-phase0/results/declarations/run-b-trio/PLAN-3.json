{
  "task": "td178",
  "latitude": {
    "genuine": true,
    "why": "Exec semantics (direct exec vs typed into a shell), how a commanded window interacts with session adoption, parser choice, and dir-flag spelling are reviewer-debatable; desktop keys and app-id plumbing are forced."
  },
  "answers": {
    "q1": {
      "position": "Accept `-e`; all remaining argv is the command, exec'd directly as the first pane's PTY child via tty::Options.shell (spawn_in gains a program parameter). No shell wrapper; the window closes when it exits.",
      "surface": "app/src/term.rs",
      "rationale": "TD_DEMO already runs a program via Options.shell; direct exec is foot/alacritty parity — args and exit survive unmangled."
    },
    "q2": {
      "position": "Parse `--app-id=<id>` (= or space form) and plumb it into WindowOptions.app_id at cx.open_window, replacing the hard-coded \"terminal-delight\"; absent flag keeps the default.",
      "surface": "app/src/main.rs",
      "rationale": "app_id set before the window maps, so compositor rules match on first map — no post-open set_app_id race."
    },
    "q3": {
      "position": "`--dir=<dir>` — TD's own short spelling, made public solely through X-TerminalArgDir=--dir=; applied through the seed path (session::PaneRestore.cwd into term::spawn_in(cwd)).",
      "surface": "app/src/main.rs",
      "rationale": "Matches omarchy-launch-terminal's own vocabulary; the X-TerminalArgDir key exists precisely so terminals need not copy foot."
    },
    "q4": {
      "position": "`--title=<t>` overrides window_title() in TitlebarOptions at open; declared to consumers as X-TerminalArgTitle=--title= in packaging/terminal-delight.desktop.",
      "surface": "app/src/main.rs",
      "rationale": "Title already flows through TitlebarOptions; the desktop key is where xdg-terminal-exec learns the flag."
    },
    "q5": {
      "position": "Add exactly four keys to packaging/terminal-delight.desktop: X-TerminalArgExec=-e, X-TerminalArgAppId=--app-id=, X-TerminalArgTitle=--title=, X-TerminalArgDir=--dir=. Every existing key stays.",
      "surface": "packaging/terminal-delight.desktop",
      "rationale": "The four keys are the contract omarchy-launch-tui and xdg-terminal-exec consume; the Dir value follows q3's spelling."
    },
    "q6": {
      "position": "Unchanged: a bare launch adopts the most-recently-saved unheld session and restores it (instance::resolve_session); flag_reply still answers --help/--version and refuses unknown flags without opening a window.",
      "surface": "app/src/main.rs",
      "rationale": "Existing behaviour is tested and deliberate; the ticket adds surface, it must not change the default."
    },
    "q7": {
      "position": "Adopt lexopt (tiny, zero-dep) for the launch-flag surface; the verb dispatch (ctl/mcp/probe/agent-*) stays hand-rolled ahead of it, and flag_reply keeps the help/version gate.",
      "surface": "app/Cargo.toml",
      "rationale": "Mixed =/space forms, free flag order, and -e's argv-swallowing are real lexing; lexopt is auditably small."
    },
    "q8": {
      "position": "Only -e forces explicit-scratch (never adopt/persist). A --dir/--title/--app-id launch without -e behaves like a bare launch — adopts and restores the workspace session — with the dir seeding fresh panes.",
      "surface": "app/src/main.rs",
      "rationale": "omarchy-launch-terminal --dir means 'my workspace terminal, here'; only a commanded window is a dedicated app surface."
    }
  },
  "out_of_scope_touched": []
}
